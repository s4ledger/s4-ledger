"""HORIZON — source package.

The agent's runtime entry points live here. Keep this package
self-contained: no imports from the wider s4ledger repo. Everything
needed to run HORIZON must be installable from `requirements.txt` and
present under `/horizon/`.
"""

__version__ = "1.0.0"
