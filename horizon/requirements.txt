# HORIZON — Python dependencies (pinned to minor versions)
# Keep this list minimal so Replit installs in seconds.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
